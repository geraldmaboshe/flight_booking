# Flight booking App

## Backend

- `npm install` to install dependencies
- create `env` with your values
- `docker-compose up` to run postgres db and adminer
- `npm run migrate` to migrate the database
- adminer will be running at http://localhost:8080
